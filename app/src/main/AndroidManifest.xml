<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.police.police">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".WelxomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".IndexActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".page.ArraignmentRoomStatusActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".page.YuYueActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".page.DetailsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".page.MakeAnAppointmentActivity"
            android:theme="@style/AppTheme.NoActionBar" />
<!-- 通知通报Activity-->
        <activity
            android:name=".announcement.NoticeAnnouncementActivity"
            android:theme="@style/AppTheme.NoActionBar" />
<!-- 110接处警Activity-->
        <activity
            android:name=".announcement.activity.Alarm110Activity"
            android:theme="@style/AppTheme.NoActionBar"/>
    </application>

</manifest>
